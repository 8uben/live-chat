<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>LiveChat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-gray-800">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center">
            <%= link_to 'LiveChat', root_path, class: 'bg-gray-900 text-white rounded-md px-3 py-2 text-sm font-medium', aria: { current: 'page' } %>
            <%= link_to_if user_signed_in?, 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'bg-gray-900 text-white rounded-md px-3 py-2 text-sm font-medium' %>
          </div>
        </div>
      </nav>
      <main>
        <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
