<%= form_with model: Room.new, url: rooms_path do |f| %>
  <%= f.text_field :title, class: 'max-w-full' %>
  <%= f.submit 'Create room', class: 'bg-gray-100 px-3 py-2 border border-current pointer hover:bg-gray-200 cursor-pointer' %>
<% end %>
